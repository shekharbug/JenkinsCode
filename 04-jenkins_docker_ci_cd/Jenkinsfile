pipeline {
    agent any
    stages {
        stage('checkout scm'){
            steps{
                echo "doing checkout of scm git"
                checkout scm
            }
        }
    }
}